#pragma once

/**
 * @file secrets.h
 * @brief Credentials and sensitive configuration for NEXORA.
 *
 * Copy this file to secrets.h and enter your actual credentials.
 * secrets.h is listed in .gitignore to avoid committing sensitive information.
 */

// Default Wi-Fi Credentials (can be updated via MQTT / Web interface)
#define DEFAULT_WIFI_SSID       "YOUR_WIFI_SSID"
#define DEFAULT_WIFI_PASSWORD   "YOUR_WIFI_PASSWORD"

// Default MQTT Broker Configuration (e.g. HiveMQ Cloud, Mosquitto, EMQX)
#define DEFAULT_MQTT_BROKER     "broker.hivemq.com"
#define DEFAULT_MQTT_PORT       1883
#define DEFAULT_MQTT_USER       ""
#define DEFAULT_MQTT_PASSWORD   ""

// OTA Update Security
#define DEFAULT_OTA_PASSWORD    "nexora_admin_ota"
